# CLAUDE.md — PKG 구축통합관리

더존비즈온 PKG사업본부 A10(Amaranth10) 구축업무 통합 관리지표 플랫폼.
메인 산출물은 `pkg-icm/` 폴더에 있습니다.

---

## ★ 작업 완료 시 항상 지킬 것

작업이 완성되면 **결과 보고와 함께 아래 4단계 운영 절차를 반드시 같이 안내**합니다.
사용자가 매번 묻지 않아도 되도록, 완료 보고에 기본 포함합니다.

```
① 배포본 생성   python update.py <GCMS.xlsx> [--assignee <담당자별.xlsx>] [--capa <CAPA.xlsx>]
                                   [--foex <FoEX교육실적.xlsx>] [--ext <ext_perf.json>]
                 → ETL(항등식 검증) → index_standalone.html → artifact.html 일괄 생성
② 데이터 갱신   원본 엑셀만 바꿔 ①을 다시 실행. 파일명 YYMMDD 를 기준일로 자동 인식
③ 실행         index_standalone.html 더블클릭  (웹서버·인터넷 불필요)
④ 링크 재배포   Claude Code 세션에서 "artifact.html 재배포해줘"
                 → 같은 URL로 갱신 (새 링크 생기지 않음)
```

**배포 URL** — https://claude.ai/code/artifact/6d3c599b-120f-4cb7-bad8-c5efd0cde218

재배포 시 주의: 이 대화에서는 같은 파일 경로로 재배포하면 URL이 유지됩니다.
**다른 대화에서 배포할 때는 위 URL을 함께 전달**해야 같은 링크가 유지되고, 아니면 새 링크가 생깁니다.

---

## 산출 정책 — 임의로 바꾸지 말 것

### 계약공수 기준 공수 산정 (작업지침 v2 §1-3 확정)

NSM 시스템 개발 시 그대로 구현될 정책입니다. 260724 확정 실측값을 항등식 포함 전 셀 재현합니다.

```
계약공수 분기 (Y열 구축구분)
  FoEX교육(1:N)       계약공수=표준공수(AH)  미투입 = 계약공수 × 잔여율        효율계수 ×30%
  FoEX교육(1:N)+방문   계약공수=표준공수(AH)  미투입 = 유상(AI−AJ) + 무상×잔여율  무상분만 ×30%
  방문구축 / FoEX(단독) 계약공수=예상공수(AI)  미투입 = AI − AJ

항등식  계약공수 = 투입환산 + 미투입1차        (7,105.0 = 2,363.9 + 4,741.1)
       최종미투입 = 유상 + 무상×30%          (3,976.6)
       구축지연 = 최종미투입 ÷ 월가용CAPA      (2.20M = 3,976.6 ÷ 1,804)
```

**문서만으로 재현되지 않아 역산으로 확정한 조건 2건 — 절대 변경 금지**

1. 구축진행율(가상)은 **상한 1.0만 적용**. 착수 전(음수 진행율) 20건을 0으로 하한 클램프하면 안 됨
2. 특수규칙 ②(완료예정일 경과 → 잔여율 5%)는 **FoEX 1:N계열에만 적용** (전체 적용 시 57건, 1:N계열만 23건)

### 핵심 지표 우선순위

| 순위 | KPI | 산출 | 비고 |
|---|---|---|---|
| 1 | **구축기간 준수율** (1.9) | 구축완료예정일(AD) 단독 기준 | 대표 지표 |
| 2 | **WBS 준수율** (4.6) | 순공수 / 개별 예상공수 | 순공수 = 투입+추가+마이그+아웃 |
| 3 | 납기준수율 (1.7) | 변경완료예정일(AE)→AD | 주간보고 대표값 |
| — | 계약기간준수율 (1.8) | 계약종료일(AZ)·설치형 | **참고지표. 차이 추적 금지** |

### TTV(개통 소요기간) 기산점

**구축접수일(Z열) → 구축완료일(AF열)** 기준입니다. 수주일(N열)을 기산점으로 쓰지 않습니다.
수주일 기준으로 구축접수가 이루어지지 않기 때문에, 수주일 기산 시 영업 리드타임이 섞여
구축 소요기간이 과대 계상됩니다. (수주 기산 129일 vs 접수 기산 105일)

**계약기간준수율을 추적하지 않는 이유** — 계약시작일·종료일은 구축자가 NSM·계약서를 기반으로
GCMS에 등록하는데, 영업에서 진행하는 계약정보 변경이력이 실시간 공유되지 않아 오입력이 발생할 수
있습니다. 설치형이 계약기간을 적용받을 수 있어 확인하는 보조 지표일 뿐이며, 지침 확정값과의
소수 건 차이는 데이터 품질 이슈이므로 원인을 추적하지 않습니다.

### 구축인력풀 · CAPA 산출 기준

가용 판정은 「구축인력 CAPA 관리」 ②인력마스터 + ①가용판정규칙을 그대로 구현합니다.

```
우선순위  1 구축제외  구축직무=N · 제외그룹(본부장·영업구축지원·옴니UC·인턴)
         2 비가용    비가용사유 보유 (휴직·육아·출산·공통업무 등)
         3 평가중    전환배치 & 기준일 < 배치일 + 평가기간(1개월)
         4 가용      그 외 (평가완료 전환배치 포함)
```

인력 추가·수정·상태 직접 지정이 화면에서 가능하며(변경분은 localStorage 보관, 원본 불변),
변경 시 가용 판정·센터/직급 집계·CAPA·KPI 4.5 참고값이 즉시 재계산됩니다.

**KPI 산출에는 현행 스킬값 82명을 유지합니다.** 인력풀 판정 결과(가용 89명 / 1,958 m/d)는
참고로 병기만 합니다. 인력풀 기준일(2026-07-04)이 GCMS(2026-07-24)보다 3주 이르고,
82명을 바꾸면 확정 실측값 구축지연 2.20M 재현이 깨지기 때문입니다.

### 이월 물량(백로그) 해석 원칙

Pillar 6은 **납기 리스크(물량)** 와 **품질**을 분리합니다. 이월 부담률(6.1)은 **품질 지표가 아닙니다.**

- 2025년 하반기 **업셀 전환 프로모션**으로 구축인원 대비 약 3배수 접수가 집중되었고,
  그 물량이 자연 이월된 것입니다 (이월 919건 중 99.5%가 2025년 접수)
- 2026년 들어 완료(월 229건)가 접수(월 206건)를 상회하며 재공이 **885 → 753건으로 소화 중**
- 따라서 **구축지연(6.5) 리스크로만 해석**하고 품질 저하·처리능력 부족으로 해석하지 않습니다
- 목표치(구 60% 이하)는 물량 특성상 관리 기준으로 부적합하여 제거, `추세 관리` 로 표기합니다

### 교육 · AI · 영업 성과 원천 (GCMS 외 4종)

기준일이 원천마다 다르므로 **값을 맞추지 말고 화면에 기준일을 병기**합니다 (작업지침 §2-3).

| 원천 | 연결 | 기준일 | 비고 |
|---|---|---|---|
| FoEX 교육실적 | `--foex <xlsx>` | GCMS와 동일 | RowData 원시행 직접 집계 |
| ONE AI 구축실적 | `data/ext_perf.json` | 2026-07-30 | PDF 산출값 |
| ONE AI 해지방어 | `data/ext_perf.json` | 2026-07-31 | 주차별 |
| 영업지원·업무지원 | `data/ext_perf.json` | 2026-07-16 | PDF 산출값 |

**FoEX 교육분류 — 워크북 블록 구성을 그대로 따른다**

```
정규교육과정   기초설정교육 + 관리자교육 + Fast-Track       842건
지원(특별)교육  Q&A Day(Setting)·Q&A Day(실전적용)·ONE AI    45건
                                                  1:N 합계 887건
```

`Fast-Track` 은 지원교육이 아니라 **정규교육과정 소계에 포함**됩니다. 화면 각주에 "특별 과정 =
Q&A Day + Fast-Track" 으로 표기된 자료가 있으나, 워크북 블록·합계(842/45)와 맞지 않으므로
워크북 기준을 따릅니다.

**요약 시트가 아닌 RowData 원시행에서 진행상태 `완료` 를 직접 집계**합니다. 요약 시트는 갱신이
밀리는 경우가 있습니다(단독 2·4·5월이 원시행과 불일치).

**ONE AI PDF 의 요금제 `총계` 열은 갱신이 밀린 값입니다.** 월별 합(921)이 구축완료(921)와
일치하므로 **월별 합을 사용**합니다. (PDF 총계 818 — 사용 금지)

FoEX 교육실적 워크북은 피벗캐시가 깨져 있어 openpyxl 로 바로 열리지 않습니다.
`open_no_pivot()` 이 pivot 파트를 제거한 임시 사본을 만들어 엽니다 (원본 불변).

### 금지값 (작업지침 §5-2)

`납기준수율 이진판정 70.0%/71.5%` · `계약기간준수율 95.3%` · `미투입공수 단순 AI−AJ` ·
`v69 값 3,747 / 2.08M` · `AK(진행률%) 컬럼` · `CAPA 가용 94명(실질 82명)` ·
`수주금액을 P열(라이선스)로 산출(O열 총수주액 사용)`

---

## 구조

```
pkg-icm/
├── update.py                ★ 원클릭 파이프라인 (ETL → 실행본 → 배포본)
├── etl_gcms.py              원본 엑셀 → data/gcms_full.json + 항등식 검증
├── build_standalone.py      → index_standalone.html (더블클릭 실행본)
├── build_artifact.py        → artifact.html (웹 배포본, 외부요청 0건)
├── js/{ingest,bulk,pool,data,kpi,views,app}.js
│   ※ bulk.js = etl_gcms.py 산식의 JS 포트. 두 곳을 항상 함께 수정할 것
│   ※ etl_gcms.py 는 --assignee(담당자별) · --capa(인력풀) 옵션으로 원천 3종을 함께 로드
├── data/gcms_full.json      프로젝트 2,407건 + 배정 7,456행
└── data/ext_perf.json       ONE AI 구축·해지방어·영업지원 산출값 (원본 PDF 갱신 시 수치+asOf 동시 수정)
```

- 화면 11종 / KPI 36개 (자동산출 20 · 대체산출 4 · 연동필요 12)
- 원천: GCMS `GCMS A10(통합)구축진행현황` 시트 + 담당자별 상세
- 소스별 기준일이 다르면 **값을 맞추지 말고 기준일을 병기**한다 (작업지침 §2-3)

## 작업 규칙

- 목록 필터에 `input` 과 `change` 를 함께 바인딩하지 마십시오. 텍스트 입력에서 blur 시 change 가
  발생해 목록이 다시 그려지고 **직후의 클릭이 취소**됩니다. `bindFilters()` 를 사용하십시오. (실제 발생한 버그)
- Python heredoc 안에서 JS/Python 문자열을 생성할 때 `re.sub` 치환문의 `\n` 은 개행으로 해석됩니다.
  치환문에는 raw string을 쓰거나 Edit 도구를 사용하십시오. (실제로 2회 발생한 사고)
- 변경 후에는 `python update.py <원본>` 로 항등식 검증이 통과하는지 확인합니다.
- 브라우저 검증은 playwright(`/opt/pw-browsers/chromium`)로 8개 탭 렌더링 + JS 오류 0을 확인합니다.
