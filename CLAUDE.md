# CLAUDE.md — PKG 구축통합관리

더존비즈온 PKG사업본부 A10(Amaranth10) 구축업무 통합 관리지표 플랫폼.
메인 산출물은 `pkg-icm/` 폴더에 있습니다.

---

## ★ 작업 완료 시 항상 지킬 것

작업이 완성되면 **결과 보고와 함께 아래 4단계 운영 절차를 반드시 같이 안내**합니다.
사용자가 매번 묻지 않아도 되도록, 완료 보고에 기본 포함합니다.

```
① 배포본 생성   python update.py <GCMS.xlsx> [--assignee <담당자별.xlsx>]
                 → ETL(항등식 검증) → index_standalone.html → artifact.html 일괄 생성
② 데이터 갱신   원본 엑셀만 바꿔 ①을 다시 실행. 파일명 YYMMDD 를 기준일로 자동 인식
③ 실행         index_standalone.html 더블클릭  (웹서버·인터넷 불필요)
④ 링크 재배포   Claude Code 세션에서 "artifact.html 재배포해줘"
                 → 같은 URL로 갱신 (새 링크 생기지 않음)
```

**배포 URL** — https://claude.ai/code/artifact/6d3c599b-120f-4cb7-bad8-c5efd0cde218

재배포 시 주의: 이 대화에서는 같은 파일 경로로 재배포하면 URL이 유지됩니다.
**다른 대화에서 배포할 때는 위 URL을 함께 전달**해야 같은 링크가 유지되고, 아니면 새 링크가 생깁니다.

---

## 산출 정책 — 임의로 바꾸지 말 것

### 계약공수 기준 공수 산정 (작업지침 v2 §1-3 확정)

NSM 시스템 개발 시 그대로 구현될 정책입니다. 260724 확정 실측값을 항등식 포함 전 셀 재현합니다.

```
계약공수 분기 (Y열 구축구분)
  FoEX교육(1:N)       계약공수=표준공수(AH)  미투입 = 계약공수 × 잔여율        효율계수 ×30%
  FoEX교육(1:N)+방문   계약공수=표준공수(AH)  미투입 = 유상(AI−AJ) + 무상×잔여율  무상분만 ×30%
  방문구축 / FoEX(단독) 계약공수=예상공수(AI)  미투입 = AI − AJ

항등식  계약공수 = 투입환산 + 미투입1차        (7,105.0 = 2,363.9 + 4,741.1)
       최종미투입 = 유상 + 무상×30%          (3,976.6)
       구축지연 = 최종미투입 ÷ 월가용CAPA      (2.20M = 3,976.6 ÷ 1,804)
```

**문서만으로 재현되지 않아 역산으로 확정한 조건 2건 — 절대 변경 금지**

1. 구축진행율(가상)은 **상한 1.0만 적용**. 착수 전(음수 진행율) 20건을 0으로 하한 클램프하면 안 됨
2. 특수규칙 ②(완료예정일 경과 → 잔여율 5%)는 **FoEX 1:N계열에만 적용** (전체 적용 시 57건, 1:N계열만 23건)

### 핵심 지표 우선순위

| 순위 | KPI | 산출 | 비고 |
|---|---|---|---|
| 1 | **구축기간 준수율** (1.9) | 구축완료예정일(AD) 단독 기준 | 대표 지표 |
| 2 | **WBS 준수율** (4.6) | 순공수 / 개별 예상공수 | 순공수 = 투입+추가+마이그+아웃 |
| 3 | 납기준수율 (1.7) | 변경완료예정일(AE)→AD | 주간보고 대표값 |
| — | 계약기간준수율 (1.8) | 계약종료일(AZ)·설치형 | **참고지표. 차이 추적 금지** |

**계약기간준수율을 추적하지 않는 이유** — 계약시작일·종료일은 구축자가 NSM·계약서를 기반으로
GCMS에 등록하는데, 영업에서 진행하는 계약정보 변경이력이 실시간 공유되지 않아 오입력이 발생할 수
있습니다. 설치형이 계약기간을 적용받을 수 있어 확인하는 보조 지표일 뿐이며, 지침 확정값과의
소수 건 차이는 데이터 품질 이슈이므로 원인을 추적하지 않습니다.

### 금지값 (작업지침 §5-2)

`납기준수율 이진판정 70.0%/71.5%` · `계약기간준수율 95.3%` · `미투입공수 단순 AI−AJ` ·
`v69 값 3,747 / 2.08M` · `AK(진행률%) 컬럼` · `CAPA 가용 94명(실질 82명)` ·
`수주금액을 P열(라이선스)로 산출(O열 총수주액 사용)`

---

## 구조

```
pkg-icm/
├── update.py                ★ 원클릭 파이프라인 (ETL → 실행본 → 배포본)
├── etl_gcms.py              원본 엑셀 → data/gcms_full.json + 항등식 검증
├── build_standalone.py      → index_standalone.html (더블클릭 실행본)
├── build_artifact.py        → artifact.html (웹 배포본, 외부요청 0건)
├── js/{ingest,data,kpi,views,app}.js
└── data/gcms_full.json      프로젝트 2,360건 + 배정 7,456행
```

- 화면 8종 / KPI 36개 (자동산출 20 · 대체산출 4 · 연동필요 12)
- 원천: GCMS `GCMS A10(통합)구축진행현황` 시트 + 담당자별 상세
- 소스별 기준일이 다르면 **값을 맞추지 말고 기준일을 병기**한다 (작업지침 §2-3)

## 작업 규칙

- Python heredoc 안에서 JS/Python 문자열을 생성할 때 `re.sub` 치환문의 `\n` 은 개행으로 해석됩니다.
  치환문에는 raw string을 쓰거나 Edit 도구를 사용하십시오. (실제로 2회 발생한 사고)
- 변경 후에는 `python update.py <원본>` 로 항등식 검증이 통과하는지 확인합니다.
- 브라우저 검증은 playwright(`/opt/pw-browsers/chromium`)로 8개 탭 렌더링 + JS 오류 0을 확인합니다.
